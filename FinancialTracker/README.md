# FinancialTracker (Flask + MongoDB)

Вебзастосунок для управління особистими фінансами з аналітикою витрат. Проєкт реалізує повний цикл обробки даних: від збору транзакцій користувача до побудови агрегованих звітів.

### 🛠 Технічні особливості (Backend & Data):
* **NoSQL Database Integration:** Використання **MongoDB** для збереження даних. Реалізовано складні запити для фільтрації транзакцій (за датою, категорією, типом) безпосередньо на рівні бази даних.
* **Data Aggregation:** Використання **MongoDB Aggregation Pipeline** (`$match`, `$group`, `$sum`) для обчислення аналітики витрат на стороні сервера, що розвантажує клієнтську частину.
* **Security Architecture:** Реалізовано систему авторизації з хешуванням паролів (`werkzeug.security`) та механізм відновлення доступу через Email-токени (`itsdangerous`, `flask_mail`).
* **Custom Decorators:** Власні декоратори `@loginRequired` та `@completeProfileRequired` для управління доступом до маршрутів.

### 📋 Функціонал:
* **Dashboard:** Динамічне відображення балансу, доходів та витрат.
* **Smart Filtering:** Гнучка система пошуку транзакцій за ключовими словами (Regex), категоріями та часовими проміжками.
* **User Profile:** Налаштування часових поясів (`zoneinfo`) та локалізації користувача.

### 🚀 Stack:
* **Backend:** Python (Flask, Jinja2)
* **Database:** MongoDB (PyMongo)
* **Security:** Werkzeug, URLSafeTimedSerializer
* **Frontend:** HTML/CSS (Dynamic Templates)
